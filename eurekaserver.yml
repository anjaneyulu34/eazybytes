server:
  port: 8070

eureka:
  instance:
    hostname: localhost
  client:
    fetchRegistry: false
    registerWithEueka: false
    serviceUrl:
      defualtZone: http://${eureka.instance.hostname}:${server.port}/eureka/
